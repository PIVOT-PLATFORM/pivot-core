package fr.pivot.collaboratif.session.brainstorm;

/**
 * The five post-it colours a BRAINSTORM card may take (US19.3.4) — a fixed, server-validated
 * palette, so a card can never carry an arbitrary client-supplied colour string.
 */
public enum BrainstormCardColor {
    YELLOW,
    PINK,
    BLUE,
    GREEN,
    ORANGE
}
